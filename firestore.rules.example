rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /ban_katalog/{document} {
      allow read, write: if true;
    }
    match /ban_katalog_backups/{document} {
      allow read, write: if true;
    }
  }
}
